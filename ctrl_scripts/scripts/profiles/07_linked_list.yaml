# Profile for tasks/commands in order

# = Prepare cluster =
- name: update repo
  job: update
  per_command_delay: 1
  post_delay: 60

# = Run test =
- name: run linked list test
  job: linked_list
  per_command_delay: 10
  post_delay: 0

